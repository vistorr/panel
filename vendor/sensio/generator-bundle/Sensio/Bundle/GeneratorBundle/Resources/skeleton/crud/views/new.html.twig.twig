{{ "{% extends 'PanelCtrlwBundle::layout.html.twig' %}" }}

{{ "{% form_theme form 'PanelCtrlwBundle:FormTheme:fields.html.twig' %}" }}

{{ "{% block title -%}" }}
{{ "{{ parent() }} - " ~ entity|title ~ "Crear" }}
{{ "{% endblock -%}" }}

{{ "{% block javascripts %}" }}
{{ "    {{ parent() }}" }}
{{ "    {{ form_javascript(form) }}" }}
{{ "{% endblock %}" }}

{{ "{% block sidebar -%}" }}
    {% include 'views/others/sidebar.html.twig.twig' -%}
{{ "{% endblock -%}" }}

{{ "{% block controller_name %}" }}
    {{ entity|title }}
{{ "{% endblock %}" }}

{{ "{% block batch_actions -%}" }}
    <ul>
        {% set hide_edit, hide_delete, hide_save = true, true, false %}
        {% include 'views/others/record_actions.html.twig.twig' -%}
    </ul>
{{ "{% endblock -%}" }}


{{ "{% block body -%}" }}
    <form id='{{ 'ctrlw_' ~ route_name_prefix ~ '_create' }}' action="{{ "{{ path('ctrlw_" ~ route_name_prefix ~ "_create') }}" }}" method="post" {{ "{{ form_enctype(form) }}" }}>
        <fieldset id="sf_fieldset_info_general">
            <h2>Info General</h2>
            {{ "{{ form_widget(form) }}" }}
        </fieldset>
    </form>
{{ "{% endblock -%}" }}
